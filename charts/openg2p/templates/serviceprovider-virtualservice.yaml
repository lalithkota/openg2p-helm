{{- if .Values.serviceProviderIngress.enabled }}
{{- if eq .Values.serviceProviderIngress.type "istio" }}
{{- if .Values.serviceProviderIngress.istio.virtualservice.enabled }}
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: {{ include "openg2p.fullname" . }}-service-provider
  labels:
    {{- include "openg2p.labels" . | nindent 4 }}
spec:
  hosts:
  - {{ default .Values.global.serviceProviderHostname .Values.serviceProviderIngress.istio.virtualservice.host | quote }}
  gateways:
  - {{ default (printf "%s-service-provider" (include "openg2p.fullname" .)) .Values.serviceProviderIngress.istio.virtualservice.gateway }}
  http:
  - match:
    - uri:
        exact: /
    - uri:
        exact: /my
    - uri:
        prefix: /my/
    - uri:
        exact: /web
    - uri:
        exact: /web/login
    - uri:
        exact: /web/database/selector
    redirect:
      uri: /serviceprovider
  - match:
    - uri:
        prefix: /selfservice
    redirect:
      authority: {{ default .Values.global.selfServiceHostname .Values.selfServiceIngress.istio.virtualservice.host | quote }}
  - headers:
      request:
        set:
          x-forwarded-host: {{ default .Values.global.serviceProviderHostname .Values.serviceProviderIngress.istio.virtualservice.host | quote }}
          x-forwarded-proto: https
    route:
    - destination:
        host: {{ tpl .Values.serviceProviderIngress.istio.virtualservice.destination $ }}
        port:
          number: {{ tpl .Values.serviceProviderIngress.istio.virtualservice.destinationPort $ }}
{{- end }}
{{- end }}
{{- end }}
