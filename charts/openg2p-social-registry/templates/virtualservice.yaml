{{- if .Values.istio.enabled }}
{{- if .Values.istio.virtualservice.enabled }}
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: {{ include "socialRegistry.fullname" . }}
  labels:
    {{- include "socialRegistry.labels" . | nindent 4 }}
spec:
  hosts:
  - {{ default .Values.global.hostname .Values.istio.virtualservice.host }}
  gateways:
  - {{ default (include "socialRegistry.fullname" .) .Values.istio.virtualservice.gateway }}
  http:
  - match:
    - uri:
        exact: /
    redirect:
      uri: /web
  - headers:
      request:
        set:
          x-forwarded-host: {{ default .Values.global.hostname .Values.istio.virtualservice.host }}
          x-forwarded-proto: https
    route:
    - destination:
        host: {{ .Release.Name }}-odoo
        port:
          number: {{ .Values.odoo.service.port }}
{{- end }}
{{- end }}